# API Server dependencies (minimal for deployment)
flask==3.0.0
flask-cors==4.0.0
gunicorn==21.2.0

# Optional: Only needed if running scraper on server (not recommended)
# selenium and lxml are only needed for local scraping
# beautifulsoup4==4.12.2
# lxml==5.1.0
# selenium==4.16.0
# requests==2.31.0
