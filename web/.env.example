# Database - Render will provide this automatically
DATABASE_URL="postgresql://user:password@host:5432/mention_markets?schema=public"

# Admin auth - CHANGE THESE!
ADMIN_PASSWORD="changeme-admin-2024"
ADMIN_SESSION_SECRET="changeme-random-session-secret-at-least-32-chars-long"

# Generate a secure session secret with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
